import { StackPage } from './app.po';

describe('stack App', () => {
  let page: StackPage;

  beforeEach(() => {
    page = new StackPage();
  });

  it('should expect true to be true', () => {
    expect(true).toBe(true);
  });
});
